# 011. Phase 8: 배포 준비

## 📋 작업 개요
**목표**: 앱 스토어 배포를 위한 최종 준비 및 마케팅 자료 제작
**예상 기간**: 3일
**우선순위**: P0 (필수)

## 🎯 작업 목표
1. 온보딩 화면 구현
2. 앱 아이콘 및 스플래시 화면 제작
3. 다크모드 완벽 지원
4. 접근성 테스트 및 개선
5. 스토어 등록 및 배포

## 📂 생성할 파일 구조
```
lib/
└── features/
    └── onboarding/
        └── presentation/
            ├── screens/
            │   └── onboarding_screen.dart
            └── widgets/
                ├── onboarding_page.dart
                ├── page_indicator.dart
                └── permission_request_card.dart

assets/
├── icons/
│   ├── app_icon.png (1024x1024)
│   ├── app_icon_ios.png
│   └── app_icon_android.png
├── images/
│   ├── splash_logo.png
│   ├── onboarding_1.png
│   ├── onboarding_2.png
│   └── onboarding_3.png
└── screenshots/
    ├── ios/
    │   ├── iPhone_14_Pro_Max/
    │   ├── iPhone_14_Pro/
    │   ├── iPhone_SE/
    │   └── iPad_Pro/
    └── android/
        ├── phone/
        └── tablet/

docs/
├── privacy_policy_ko.md
├── privacy_policy_en.md
├── terms_of_service_ko.md
└── app_store_description.md
```

## 🔧 상세 구현 계획

### 1. 온보딩 화면
```dart
// onboarding_screen.dart
class OnboardingScreen extends StatefulWidget {
  @override
  _OnboardingScreenState createState() => _OnboardingScreenState();
}

class _OnboardingScreenState extends State<OnboardingScreen> {
  final PageController _pageController = PageController();
  int _currentPage = 0;

  final List<OnboardingData> _pages = [
    OnboardingData(
      title: '심플한 습관 관리',
      description: '복잡한 기능 없이\n오직 습관 체크에만 집중하세요',
      image: 'assets/images/onboarding_1.png',
      backgroundColor: Color(0xFF6366F1),
    ),
    OnboardingData(
      title: '3초 루틴 체크',
      description: '홈 화면 위젯으로\n앱을 열지 않고도 빠르게 체크',
      image: 'assets/images/onboarding_2.png',
      backgroundColor: Color(0xFF8B5CF6),
    ),
    OnboardingData(
      title: '동기부여 알림',
      description: '적절한 리마인더와\n연속 달성 축하로 동기부여',
      image: 'assets/images/onboarding_3.png',
      backgroundColor: Color(0xFF10B981),
    ),
  ];

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Stack(
        children: [
          // 페이지 뷰
          PageView.builder(
            controller: _pageController,
            onPageChanged: (index) {
              setState(() => _currentPage = index);
            },
            itemCount: _pages.length,
            itemBuilder: (context, index) {
              return OnboardingPage(data: _pages[index]);
            },
          ),

          // 인디케이터
          Positioned(
            bottom: 120,
            left: 0,
            right: 0,
            child: Row(
              mainAxisAlignment: MainAxisAlignment.center,
              children: List.generate(
                _pages.length,
                (index) => AnimatedContainer(
                  duration: Duration(milliseconds: 300),
                  margin: EdgeInsets.symmetric(horizontal: 4),
                  width: _currentPage == index ? 24 : 8,
                  height: 8,
                  decoration: BoxDecoration(
                    color: _currentPage == index
                        ? Colors.white
                        : Colors.white.withOpacity(0.4),
                    borderRadius: BorderRadius.circular(4),
                  ),
                ),
              ),
            ),
          ),

          // 버튼
          Positioned(
            bottom: 40,
            left: 20,
            right: 20,
            child: Row(
              mainAxisAlignment: MainAxisAlignment.spaceBetween,
              children: [
                // Skip 버튼
                TextButton(
                  onPressed: _skipOnboarding,
                  child: Text(
                    '건너뛰기',
                    style: TextStyle(color: Colors.white70),
                  ),
                ),

                // Next/Start 버튼
                ElevatedButton(
                  onPressed: _currentPage == _pages.length - 1
                      ? _completeOnboarding
                      : _nextPage,
                  style: ElevatedButton.styleFrom(
                    backgroundColor: Colors.white,
                    foregroundColor: _pages[_currentPage].backgroundColor,
                    padding: EdgeInsets.symmetric(horizontal: 32, vertical: 12),
                  ),
                  child: Text(
                    _currentPage == _pages.length - 1 ? '시작하기' : '다음',
                    style: TextStyle(fontWeight: FontWeight.bold),
                  ),
                ),
              ],
            ),
          ),
        ],
      ),
    );
  }

  void _nextPage() {
    _pageController.nextPage(
      duration: Duration(milliseconds: 300),
      curve: Curves.easeInOut,
    );
  }

  void _skipOnboarding() {
    _completeOnboarding();
  }

  void _completeOnboarding() async {
    // 온보딩 완료 저장
    final prefs = await SharedPreferences.getInstance();
    await prefs.setBool('hasCompletedOnboarding', true);

    // 권한 요청 화면으로
    Navigator.pushReplacement(
      context,
      MaterialPageRoute(builder: (_) => PermissionRequestScreen()),
    );
  }
}
```

### 2. 권한 요청 화면
```dart
// permission_request_screen.dart
class PermissionRequestScreen extends StatefulWidget {
  @override
  _PermissionRequestScreenState createState() =>
      _PermissionRequestScreenState();
}

class _PermissionRequestScreenState extends State<PermissionRequestScreen> {
  bool _notificationGranted = false;
  bool _isRequesting = false;

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Padding(
          padding: EdgeInsets.all(24),
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              Icon(
                Icons.notifications_outlined,
                size: 80,
                color: Theme.of(context).primaryColor,
              ),
              SizedBox(height: 24),
              Text(
                '알림 권한 설정',
                style: Theme.of(context).textTheme.headlineSmall,
              ),
              SizedBox(height: 16),
              Text(
                '루틴 리마인더와 동기부여 메시지를 받으려면\n알림 권한이 필요합니다.',
                textAlign: TextAlign.center,
                style: TextStyle(color: Colors.grey[600]),
              ),
              SizedBox(height: 48),

              // 권한 카드
              Card(
                child: ListTile(
                  leading: Icon(
                    _notificationGranted
                        ? Icons.check_circle
                        : Icons.notifications_none,
                    color: _notificationGranted ? Colors.green : null,
                  ),
                  title: Text('알림 권한'),
                  subtitle: Text(
                    _notificationGranted ? '허용됨' : '권한을 허용해주세요',
                  ),
                  trailing: _isRequesting
                      ? CircularProgressIndicator()
                      : Switch(
                          value: _notificationGranted,
                          onChanged: (value) => _requestPermission(),
                        ),
                ),
              ),

              Spacer(),

              // 계속 버튼
              SizedBox(
                width: double.infinity,
                child: ElevatedButton(
                  onPressed: _continue,
                  child: Text('계속하기'),
                ),
              ),

              TextButton(
                onPressed: _skipPermission,
                child: Text(
                  '나중에 설정',
                  style: TextStyle(color: Colors.grey),
                ),
              ),
            ],
          ),
        ),
      ),
    );
  }

  Future<void> _requestPermission() async {
    setState(() => _isRequesting = true);

    final granted = await PermissionService.requestNotificationPermission(context);

    setState(() {
      _notificationGranted = granted;
      _isRequesting = false;
    });
  }

  void _continue() {
    Navigator.pushReplacementNamed(context, '/home');
  }

  void _skipPermission() {
    Navigator.pushReplacementNamed(context, '/home');
  }
}
```

### 3. 스플래시 화면
```dart
// splash_screen.dart
class SplashScreen extends StatefulWidget {
  @override
  _SplashScreenState createState() => _SplashScreenState();
}

class _SplashScreenState extends State<SplashScreen>
    with SingleTickerProviderStateMixin {
  late AnimationController _controller;
  late Animation<double> _fadeAnimation;
  late Animation<double> _scaleAnimation;

  @override
  void initState() {
    super.initState();

    _controller = AnimationController(
      duration: Duration(seconds: 2),
      vsync: this,
    );

    _fadeAnimation = Tween<double>(
      begin: 0.0,
      end: 1.0,
    ).animate(CurvedAnimation(
      parent: _controller,
      curve: Interval(0.0, 0.5, curve: Curves.easeIn),
    ));

    _scaleAnimation = Tween<double>(
      begin: 0.8,
      end: 1.0,
    ).animate(CurvedAnimation(
      parent: _controller,
      curve: Interval(0.0, 0.5, curve: Curves.easeOutBack),
    ));

    _controller.forward();
    _navigateToNext();
  }

  Future<void> _navigateToNext() async {
    await Future.delayed(Duration(seconds: 2));

    final prefs = await SharedPreferences.getInstance();
    final hasCompletedOnboarding = prefs.getBool('hasCompletedOnboarding') ?? false;

    if (mounted) {
      Navigator.pushReplacementNamed(
        context,
        hasCompletedOnboarding ? '/home' : '/onboarding',
      );
    }
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Theme.of(context).primaryColor,
      body: Center(
        child: AnimatedBuilder(
          animation: _controller,
          builder: (context, child) {
            return FadeTransition(
              opacity: _fadeAnimation,
              child: ScaleTransition(
                scale: _scaleAnimation,
                child: Column(
                  mainAxisAlignment: MainAxisAlignment.center,
                  children: [
                    Image.asset(
                      'assets/images/splash_logo.png',
                      width: 120,
                      height: 120,
                    ),
                    SizedBox(height: 24),
                    Text(
                      'RoutineMate',
                      style: TextStyle(
                        color: Colors.white,
                        fontSize: 32,
                        fontWeight: FontWeight.bold,
                      ),
                    ),
                    SizedBox(height: 8),
                    Text(
                      '작은 습관, 큰 변화',
                      style: TextStyle(
                        color: Colors.white70,
                        fontSize: 16,
                      ),
                    ),
                  ],
                ),
              ),
            );
          },
        ),
      ),
    );
  }

  @override
  void dispose() {
    _controller.dispose();
    super.dispose();
  }
}
```

### 4. 앱 아이콘 설정
```yaml
# pubspec.yaml
dev_dependencies:
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icons/app_icon.png"
  adaptive_icon_background: "#6366F1"
  adaptive_icon_foreground: "assets/icons/app_icon_foreground.png"
  web:
    generate: true
    image_path: "assets/icons/app_icon.png"
  windows:
    generate: true
    image_path: "assets/icons/app_icon.png"
```

### 5. 다크모드 테마
```dart
// app_theme.dart
class AppTheme {
  static ThemeData lightTheme = ThemeData(
    brightness: Brightness.light,
    primaryColor: Color(0xFF6366F1),
    scaffoldBackgroundColor: Colors.grey[50],
    colorScheme: ColorScheme.light(
      primary: Color(0xFF6366F1),
      secondary: Color(0xFF8B5CF6),
      surface: Colors.white,
      background: Colors.grey[50]!,
    ),
    appBarTheme: AppBarTheme(
      backgroundColor: Colors.white,
      foregroundColor: Colors.black,
      elevation: 0,
    ),
    cardTheme: CardTheme(
      elevation: 2,
      shape: RoundedRectangleBorder(
        borderRadius: BorderRadius.circular(12),
      ),
    ),
  );

  static ThemeData darkTheme = ThemeData(
    brightness: Brightness.dark,
    primaryColor: Color(0xFF6366F1),
    scaffoldBackgroundColor: Color(0xFF1A1A1A),
    colorScheme: ColorScheme.dark(
      primary: Color(0xFF6366F1),
      secondary: Color(0xFF8B5CF6),
      surface: Color(0xFF2D2D2D),
      background: Color(0xFF1A1A1A),
    ),
    appBarTheme: AppBarTheme(
      backgroundColor: Color(0xFF2D2D2D),
      foregroundColor: Colors.white,
      elevation: 0,
    ),
    cardTheme: CardTheme(
      elevation: 2,
      color: Color(0xFF2D2D2D),
      shape: RoundedRectangleBorder(
        borderRadius: BorderRadius.circular(12),
      ),
    ),
  );
}
```

### 6. 접근성 개선
```dart
// accessibility_wrapper.dart
class AccessibilityWrapper extends StatelessWidget {
  final Widget child;
  final String label;
  final String? hint;
  final bool isButton;
  final VoidCallback? onTap;

  const AccessibilityWrapper({
    required this.child,
    required this.label,
    this.hint,
    this.isButton = false,
    this.onTap,
  });

  @override
  Widget build(BuildContext context) {
    return Semantics(
      label: label,
      hint: hint,
      button: isButton,
      onTap: onTap,
      child: ExcludeSemantics(
        child: child,
      ),
    );
  }
}

// 사용 예시
AccessibilityWrapper(
  label: '${routine.name} 루틴, ${routine.isCompletedToday ? "완료됨" : "미완료"}',
  hint: '탭하여 완료 상태를 변경하세요',
  isButton: true,
  onTap: () => toggleRoutine(routine.id),
  child: RoutineCard(routine: routine),
)
```

### 7. 스토어 등록 정보
```markdown
# App Store Description

## 한국어

### 제목
RoutineMate - 심플한 습관 트래커

### 부제목
3초 만에 오늘의 루틴 체크

### 설명
RoutineMate는 복잡한 기능 없이 오직 습관 체크에만 집중한 미니멀한 습관 관리 앱입니다.

**주요 기능**
• 간단한 탭으로 루틴 체크
• 홈 화면 위젯으로 빠른 접근
• 시각적인 진행률 표시
• 연속 달성 스트릭 기록
• 맞춤 리마인더 알림
• 다크모드 지원

**이런 분들께 추천합니다**
• 복잡한 앱이 부담스러운 분
• 간단하게 습관을 기록하고 싶은 분
• 홈 화면에서 바로 체크하고 싶은 분

작은 습관이 모여 큰 변화를 만듭니다.
지금 바로 RoutineMate와 함께 시작하세요!

### 키워드
습관, 루틴, 트래커, 습관관리, 루틴체크, 습관형성, 자기계발, 생산성

### 카테고리
생산성 (Productivity)

### 연령 등급
4+
```

### 8. 개인정보처리방침
```markdown
# 개인정보처리방침

## 1. 수집하는 개인정보
RoutineMate는 다음과 같은 정보를 수집합니다:
- 사용자가 입력한 루틴 데이터 (로컬 저장)
- 앱 사용 통계 (익명화된 데이터)

## 2. 개인정보 이용 목적
- 서비스 제공 및 개선
- 사용자 경험 향상
- 익명화된 통계 분석

## 3. 개인정보 보관 및 파기
- 모든 데이터는 사용자 기기에 로컬 저장
- 앱 삭제 시 모든 데이터 자동 삭제

## 4. 제3자 제공
- 사용자 동의 없이 제3자에게 제공하지 않음
- Firebase Analytics를 통한 익명 통계만 수집

## 5. 사용자 권리
- 언제든지 데이터 삭제 가능
- 설정에서 분석 데이터 수집 거부 가능

문의: support@routinemate.app
최종 수정일: 2025년 11월 20일
```

## 🧪 최종 체크리스트

### 기능 테스트
- [ ] 모든 CRUD 기능 정상 동작
- [ ] 위젯 동기화 확인
- [ ] 알림 정상 발송
- [ ] 다크모드 전환
- [ ] 가로/세로 모드

### 성능 테스트
- [ ] 앱 시작 시간 < 2초
- [ ] 메모리 사용량 < 100MB
- [ ] 배터리 사용량 테스트
- [ ] 네트워크 사용량 최소화

### 접근성 테스트
- [ ] VoiceOver (iOS) 테스트
- [ ] TalkBack (Android) 테스트
- [ ] 색상 대비율 확인
- [ ] 터치 영역 크기 확인

### 스토어 준비
- [ ] 앱 아이콘 (1024x1024)
- [ ] 스크린샷 (각 기기별 5장)
- [ ] 앱 설명 번역 (한/영)
- [ ] 개인정보처리방침 URL
- [ ] 지원 URL

### 배포 전 최종
- [ ] 릴리즈 빌드 생성
- [ ] ProGuard 규칙 설정 (Android)
- [ ] 코드 서명 (iOS)
- [ ] 버전 번호 확인

## ⚠️ 주의사항
1. **스토어 정책**
   - 광고 정책 준수
   - 개인정보 정책 명시
   - 적절한 연령 등급

2. **지역화**
   - 최소 한국어/영어
   - 날짜/시간 형식
   - 통화 표시 (광고 수익)

3. **업데이트 계획**
   - 버그 수정 프로세스
   - 기능 업데이트 로드맵
   - 사용자 피드백 채널

## 🎯 완료 기준
- [ ] 온보딩 플로우 완성
- [ ] 스플래시 화면 구현
- [ ] 다크모드 완벽 지원
- [ ] 접근성 테스트 통과
- [ ] 스토어 심사 제출

## 📝 Post-Launch
- 사용자 피드백 수집
- 크래시 리포트 모니터링
- A/B 테스트 진행
- 정기 업데이트 계획